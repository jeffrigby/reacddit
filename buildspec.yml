version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      #- npm install -g aws-sam-cli

  build:
    commands:
      - sam build -c -p

artifacts:
  files:
    - template.yaml
    - packaged.yaml