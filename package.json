{
  "name": "reacddit",
  "version": "1.0.0",
  "description": "A Reddit client built in reactjs.",
  "type": "module",
  "scripts": {
    "postinstall": "npm run install-all",
    "install-all": "cd client && npm install && cd ../api && npm install && cd ../proxy && npm install",
    "setup": "tsx scripts/setup-wizard.ts",
    "start": "tsx scripts/start-dev.ts",
    "start-proxy": "cd proxy && npm start",
    "start-client": "cd client && npm start",
    "build-client": "cd client && npm run build",
    "start-api": "cd api && npm start",
    "outdated-all": "find . -maxdepth 2 -name package.json -execdir npm outdated \\;",
    "update-all": "find . -maxdepth 2 -name package.json -execdir npm update \\;"
  },
  "keywords": [
    "es6",
    "react",
    "redux",
    "reddit"
  ],
  "author": "Jeff Rigby",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@inquirer/prompts": "^8.0.1",
    "@types/node": "^24.10.1",
    "concurrently": "^9.0.1",
    "open": "^11.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
